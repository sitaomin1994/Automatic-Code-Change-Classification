{"application": "apache-commons-net", "csha": "0e2f4d6f33eb3b74142e1bc2f53dc0d292191b83", "files": [{"IMAPExportMbox_cluster": {"actions": [{"root": "UPDATE from <li>BODY.PEEK[HEADER.FIELDS (SUBJECT)]</li> to <li>'BODY.PEEK[HEADER.FIELDS (SUBJECT)]'</li>", "operations": ["UPD TextElement: <li>BODY.PEEK[HEADER.FIELDS (SUBJECT)]</li> from <li>BODY.PEEK[HEADER.FIELDS (SUBJECT)]</li> to <li>'BODY.PEEK[HEADER.FIELDS (SUBJECT)]'</li>"]}, {"root": "UPDATE from <li>(INTERNALDATE BODY.PEEK[]) - this is the default</li> to <li>'(INTERNALDATE BODY.PEEK[])' - this is the default</li>", "operations": ["UPD TextElement: <li>(INTERNALDATE BODY.PEEK[]) - this is the default</li> from <li>(INTERNALDATE BODY.PEEK[]) - this is the default</li> to <li>'(INTERNALDATE BODY.PEEK[])' - this is the default</li>"]}, {"root": "UPDATE from Note that the sequence-set and item names are passed unmodified to the FETCH command. to Note that the sequence-set is passed unmodified to the FETCH command.", "operations": ["UPD TextElement: Note that the sequence-set and item names are passed unmodified to the FETCH command. from Note that the sequence-set and item names are passed unmodified to the FETCH command. to Note that the sequence-set is passed unmodified to the FETCH command."]}, {"root": "INS TextElement: The item names are wrapped in parentheses if more than one is provided. to TagElement at 25", "operations": ["INS TextElement: The item names are wrapped in parentheses if more than one is provided. to TagElement at 25"]}, {"root": "INS TextElement: Otherwise, the parameter is assumed to be wrapped if necessary. to TagElement at 26", "operations": ["INS TextElement: Otherwise, the parameter is assumed to be wrapped if necessary. to TagElement at 26"]}, {"root": "INS TextElement: Parameters with spaces must be quoted otherwise the OS shell will normally treat them as separate parameters. to TagElement at 27", "operations": ["INS TextElement: Parameters with spaces must be quoted otherwise the OS shell will normally treat them as separate parameters. to TagElement at 27"]}, {"root": "MOVE from Block", "operations": ["MOV IfStatement to Block at 0"]}]}}, {"IMAPExportMbox_diff": {"operations": ["Update TextElement: <li>BODY.PEEK[HEADER.FIELDS (SUBJECT)]</li>(56) to <li>'BODY.PEEK[HEADER.FIELDS (SUBJECT)]'</li>", "Update TextElement: <li>(INTERNALDATE BODY.PEEK[]) - this is the default</li>(59) to <li>'(INTERNALDATE BODY.PEEK[])' - this is the default</li>", "Update TextElement: Note that the sequence-set and item names are passed unmodified to the FETCH command.(66) to Note that the sequence-set is passed unmodified to the FETCH command.", "Insert TextElement: The item names are wrapped in parentheses if more than one is provided.(67) into TagElement(72) at 25", "Insert TextElement: Otherwise, the parameter is assumed to be wrapped if necessary.(68) into TagElement(72) at 26", "Insert TextElement: Parameters with spaces must be quoted otherwise the OS shell will normally treat them as separate parameters.(69) into TagElement(72) at 27", "Move IfStatement(803) into Block(926) at 0"]}}], "commit_stats": [{"total_files": 1, "deleted_files": 0, "testing": "False", "maintenance": "False", "build": "False"}]}